The Java Collection Framework (JCF) is a unified architecture for representing and manipulating collections,
 enabling developers to store, retrieve, and manipulate data efficiently.
  It includes interfaces, classes, and algorithms.

🔷 1. Core Interfaces in Collection Framework
| Interface    | Description                                                        |
| ------------ | ------------------------------------------------------------------ |
| `Collection` | Root interface of the collection hierarchy                         |
| `List`       | Ordered collection that allows duplicate elements                  |
| `Set`        | Collection that doesn’t allow duplicates                           |
| `Queue`      | Designed for holding elements prior to processing                  |
| `Deque`      | Double-ended queue                                                 |
| `Map`        | Object that maps keys to values (not part of Collection hierarchy) |


🔷 2. Common Implementations
| Interface | Common Classes                                     |
| --------- | -------------------------------------------------- |
| List      | `ArrayList`, `LinkedList`, `Vector`, `Stack`       |
| Set       | `HashSet`, `LinkedHashSet`, `TreeSet`              |
| Queue     | `PriorityQueue`, `ArrayDeque`                      |
| Map       | `HashMap`, `TreeMap`, `LinkedHashMap`, `Hashtable` |
